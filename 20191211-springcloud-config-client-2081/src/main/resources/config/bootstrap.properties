spring.application.name=springcloud-config-client
# 需要加载哪一个配置文件必须要让client端知道
spring.cloud.config.profile=dev
# 配置所需要加载文件仓库的命名空间(如果不配则使用master)
spring.cloud.config.label=master
# 配置config-server端的地址
spring.cloud.config.uri=http://localhost:3081